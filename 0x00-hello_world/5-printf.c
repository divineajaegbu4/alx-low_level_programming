#include <stdio.h>

/**
 * main - Entry point
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
	char test_str[] = "\"with proper grammar, but the outcome is a piece of art,";

        print(test_str);
	return (0);
}
